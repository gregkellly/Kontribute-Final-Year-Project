import { ActorConfig, ActorSubclass, HttpAgentOptions, Identity } from "@dfinity/agent";
import { InterfaceFactory } from "@dfinity/candid/lib/cjs/idl";
export interface HttpAgentOptionsSyncIdentity extends HttpAgentOptions {
    identity?: Identity;
}
export interface HttpAgentOptionsWithIdentity extends HttpAgentOptions {
    identity: Identity;
}
export interface CreateActorOptions {
    IDL: InterfaceFactory;
    canisterId: string;
    agentOptions: HttpAgentOptionsSyncIdentity;
    actorConfig?: ActorConfig;
}
export declare function createActor<T>({ IDL, canisterId, agentOptions, actorConfig, }: CreateActorOptions): ActorSubclass<T>;
